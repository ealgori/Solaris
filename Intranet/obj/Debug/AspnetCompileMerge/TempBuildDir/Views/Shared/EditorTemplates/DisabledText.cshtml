@{
    string _class = "";
    if (ViewData["class"] != null)
    {
        _class = ViewData["class"].ToString();
        @Html.TextBox("", ViewData.TemplateInfo.FormattedModelValue.ToString(), new {  @readonly = "true", @class = _class, placeholder = ViewData.ModelMetadata.Watermark })
    }
    else
    {
        @Html.TextBox("", ViewData.TemplateInfo.FormattedModelValue.ToString(), new { @readonly = "true", placeholder = ViewData.ModelMetadata.Watermark }) 
    }
}

